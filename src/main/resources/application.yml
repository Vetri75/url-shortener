spring:
  data:
    redis:
      host: url-shortener-redis-nkty4w.serverless.apse2.cache.amazonaws.com
      port: 6379
      ssl:
        enabled: true
      lettuce:
        pool:
          max-active: 20
          max-idle: 10

server:
  tomcat:
    threads:
      max: 300